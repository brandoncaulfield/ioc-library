/**
 * Inject function that adds new classes or objects to a class's prototype (as dependencies)
 * @param modules
 * @returns
 */
export declare function Inject(modules: any): (target: any) => void;
